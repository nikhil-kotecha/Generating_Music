Code Cleanup
- cleaned up many issues with the input kernel (this generates the equivalent of the author's InputForm.
- I was using tf.reshape function too freely, and it wasn't slicing things up as expected.
- Verified numerically that the note stand expand puts out the right values
